/**
 * Client-safe ImageKit display primitives (@imagekit/next).
 * Do not import the Node SDK or server-only secrets here — use `imagekit-server.ts`.
 */
export { ImageKitProvider, Image, Video, buildSrc, upload } from '@imagekit/next';
